server:
  port: 8600

spring:
  application:
    name: store-service

  datasource:
    url: jdbc:mysql://localhost:3306/store_db?createDatabaseIfNotExist=true
    username: austin
    password: austinmysql

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true